


/*
const a = 10;
const b = 20;  
if (a != b) {
    soma = a + b;
    console.log("O numero encontrado foi", soma);    
}else{
    soma = a - b;
    console.log("O numro encontrado foi", soma);
} */

/*     
    
    const a = 1;
    const b = 2;
    if (a === b) {
        subtracao = a - b;
        console.log("Este foi o resultado da subtracao", subtracao);            
    } else {
        subtracao = b - a; 
        console.log("Este foi o resultado da subtracao", subtracao); 
    }  */  
   /*      let = numero = 7;
        let multiplicacao = 1;
        const a = 10;
        const b = 5;
        if (multiplicacao === numero) {
            multiplicacao = a * b;   
            console.log(multiplicacao);     
        }else{
            multiplicacao = a + multiplicacao;
            console.log(multiplicacao);
        } */
           
     /*    let divisao = 100;
        const a = 50;
        const b = 20;
        let subtotal;

       if (divisao / 10 != 1) {
           subtotal = a / b;
           console.log("Resultado obtido foi: ", subtotal);
       } else if( divisao / 10 === 10) {
        subtotal = b / a;
        console.log("Resultado da divisao foi:", subtotal);
       }else {

           console.log("O resultado encontrado esta ");

       }
         */
        
        
  /*       let modulo = 10; 
    if (modulo % 2 === 0) {
        console.log("Ok, o numero é divisivel por 2");
    }else{
            console.log("O numero não é divisivel por 2");
    }
         */

    const soma;
    let total;
 