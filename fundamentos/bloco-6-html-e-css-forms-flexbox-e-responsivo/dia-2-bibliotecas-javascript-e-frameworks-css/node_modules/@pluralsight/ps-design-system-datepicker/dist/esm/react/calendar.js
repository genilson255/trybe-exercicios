var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
/* eslint-disable @typescript-eslint/no-unnecessary-type-assertion */
import React from 'react';
import Button from '@pluralsight/ps-design-system-button';
import { CaretLeftIcon, CaretRightIcon } from '@pluralsight/ps-design-system-icon';
import { classNames, debounce, generateId } from '@pluralsight/ps-design-system-util';
import FocusManager from '@pluralsight/ps-design-system-focusmanager';
import Theme from '@pluralsight/ps-design-system-theme';
import { add, sub } from 'date-fns';
import '../css/index.css';
import { slides } from '../vars/index';
var monthNamesShort = [
    'Jan',
    'Feb',
    'Mar',
    'Apr',
    'May',
    'Jun',
    'Jul',
    'Aug',
    'Sep',
    'Oct',
    'Nov',
    'Dec'
];
var weekdayNamesShort = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
var focusForwardMonthButton = function (context) {
    var _a;
    (_a = context.querySelector('#forward-month')) === null || _a === void 0 ? void 0 : _a.focus();
};
var focusBackwardMonthButton = function (context) {
    var _a;
    (_a = context.querySelector('#backward-month')) === null || _a === void 0 ? void 0 : _a.focus();
};
export var Calendar = React.forwardRef(function (_a, forwarededRef) {
    var calendars = _a.calendars, className = _a.className, getBackProps = _a.getBackProps, getForwardProps = _a.getForwardProps, children = _a.children, _b = _a.selected, selected = _b === void 0 ? new Date() : _b, _c = _a.autofocus, autofocus = _c === void 0 ? false : _c, _d = _a.trapped, trapped = _d === void 0 ? false : _d, _e = _a.returnFocus, returnFocus = _e === void 0 ? false : _e, getDateProps = _a.getDateProps, rest = __rest(_a, ["calendars", "className", "getBackProps", "getForwardProps", "children", "selected", "autofocus", "trapped", "returnFocus", "getDateProps"]);
    var _f = React.useState(), slide = _f[0], setSlide = _f[1];
    var _g = React.useState(), height = _g[0], setHeight = _g[1];
    var _h = React.useState(selected), focusable = _h[0], setFocusable = _h[1];
    var _j = React.useState(false), monthButtonClicked = _j[0], setMonthButtonClicked = _j[1];
    var _k = getBackProps({ calendars: calendars }), onBackClick = _k.onClick, backRest = __rest(_k, ["onClick"]);
    var _l = getForwardProps({
        calendars: calendars
    }), onForwardClick = _l.onClick, forwardRest = __rest(_l, ["onClick"]);
    var headerRef = React.useRef(null);
    var handleForwardClick = function (e) {
        onForwardClick(e);
        var nextDate = add(focusable, { months: 1 });
        setFocusable(nextDate);
        setSlide(slides.forward);
        setMonthButtonClicked(true);
        var header = headerRef.current;
        header && debounce(100, focusForwardMonthButton)(header);
    };
    var handleBackClick = function (e) {
        onBackClick(e);
        var nextDate = sub(focusable, { months: 1 });
        setFocusable(nextDate);
        setSlide(slides.backward);
        setMonthButtonClicked(true);
        var header = headerRef.current;
        header && debounce(100, focusBackwardMonthButton)(header);
    };
    var handleKeyDown = function (evt) {
        var key = evt.key;
        if (key === 'Enter' || key === ' ') {
            evt.currentTarget.id === 'backward-month'
                ? handleBackClick(evt)
                : handleForwardClick(evt);
            evt.preventDefault();
        }
    };
    var animationRef = React.useRef();
    React.useEffect(function () {
        var el = animationRef.current;
        if (el) {
            var height_1 = el.getBoundingClientRect().height;
            setHeight(height_1);
        }
    }, [slide]);
    React.useEffect(function () {
        var el = animationRef.current;
        if (el) {
            var height_2 = el.getBoundingClientRect().height;
            setHeight(height_2);
            var updateOffset_1 = function () { return setSlide(undefined); };
            el.addEventListener('animationend', updateOffset_1);
            return function () { return el.removeEventListener('animationend', updateOffset_1); };
        }
    }, []);
    var gridLabels = calendars.map(function (calendar, i) {
        return generateId("psds-datepicker-grid--" + calendar.month + "/" + calendar.year + "-");
    });
    var ref = React.useRef(null);
    // eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion
    React.useImperativeHandle(forwarededRef, function () { return ref.current; });
    return calendars.length ? (React.createElement(FocusManager, __assign({}, rest, { className: classNames('psds-calendar', className), ref: forwarededRef, autofocus: autofocus, trapped: trapped, returnFocus: returnFocus }),
        React.createElement("div", { className: "psds-calendar__header-wrapper", ref: headerRef }, calendars.map(function (calendar, i) { return (React.createElement("div", { key: "" + calendar.month + calendar.year, className: "psds-calendar__header" },
            React.createElement(Theme, { name: Theme.names.light },
                React.createElement("div", { className: "psds-calendar__month" },
                    i === 0 ? (React.createElement(Button, __assign({}, backRest, { className: "psds-calendar__header-button", id: "backward-month", onClick: handleBackClick, onKeyDown: handleKeyDown, icon: React.createElement(CaretLeftIcon, null), appearance: Button.appearances.flat }))) : (React.createElement("div", { className: "psds-calendar__header-button" })),
                    React.createElement("h2", { key: "" + calendar.month + calendar.year, id: gridLabels[i], className: "psds-calendar__header-month" },
                        monthNamesShort[calendar.month],
                        " ",
                        calendar.year),
                    calendars.length - 1 === i ? (React.createElement(Button, __assign({}, forwardRest, { id: "forward-month", className: "psds-calendar__header-button", onClick: handleForwardClick, onKeyDown: handleKeyDown, icon: React.createElement(CaretRightIcon, null), appearance: Button.appearances.flat }))) : (React.createElement("div", { className: "psds-calendar__header-button" })))),
            weekdayNamesShort.map(function (weekday) { return (React.createElement("div", { key: "" + calendar.month + calendar.year + weekday, className: "psds-calendar__weekday-header" }, weekday)); }))); })),
        React.createElement("div", { style: { height: height }, className: "psds-calendar__date-grid" },
            React.createElement("div", { className: classNames('psds-calendar__grid-slide', slide === 'forward' && 'psds-calendar__grid-slide--forward', slide === 'backward' && 'psds-calendar__grid-slide--backward'), ref: animationRef }, calendars.map(function (calendar, i) { return (React.createElement("div", { className: "psds-calendar__date-grid", key: "" + calendar.month + calendar.year }, calendar.weeks.map(function (week, weekIndex) {
                return week.map(function (dateObj, index) {
                    var key = "" + calendar.month + calendar.year + weekIndex + index;
                    if (!dateObj) {
                        return (React.createElement("div", { key: key, className: classNames('psds-calendar__filler', className) }));
                    }
                    var dateProps = getDateProps({
                        dateObj: dateObj
                    });
                    return children({
                        key: key,
                        dateObj: dateObj,
                        dateProps: dateProps,
                        'aria-labelledby': gridLabels[i],
                        calendars: calendars,
                        focusable: focusable,
                        getBackProps: getBackProps,
                        getForwardProps: getForwardProps,
                        monthButtonClicked: monthButtonClicked,
                        setFocusable: setFocusable,
                        setMonthButtonClicked: setMonthButtonClicked,
                        setSlide: setSlide
                    });
                });
            }))); }))))) : null;
});
//# sourceMappingURL=calendar.js.map